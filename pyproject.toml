[project]
name = "etl-de-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "saviya98", email = "savithdissanayake98@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.12.0",
    "dbt-snowflake>=1.12.0",
]

[project.scripts]
etl-de-project = "etl_de_project:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
